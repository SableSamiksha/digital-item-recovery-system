package com.itemrecovery;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Digital Item Recovery System application.
 * Spring Boot application that handles lost and found item reporting and recovery.
 */
@SpringBootApplication
public class DigitalItemRecoveryApplication {

    public static void main(String[] args) {
        SpringApplication.run(DigitalItemRecoveryApplication.class, args);
    }
}
